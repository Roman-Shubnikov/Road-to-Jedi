import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import React from 'react';
import { Icon28ChevronBack, Icon28ChevronLeftOutline, Icon28ArrowLeftOutline } from '@vkontakte/icons';
import PanelHeaderButton from '../PanelHeaderButton/PanelHeaderButton';
import { ANDROID, VKCOM, IOS } from '../../lib/platform';
import usePlatform from '../../hooks/usePlatform';
import withAdaptivity, { SizeType } from '../../hoc/withAdaptivity';
import getClassName from '../../helpers/getClassName';
import classNames from '../../lib/classNames';

var PanelHeaderBack = function PanelHeaderBack(_ref) {
  var label = _ref.label,
      sizeX = _ref.sizeX,
      props = _objectWithoutProperties(_ref, ["label", "sizeX"]);

  var platform = usePlatform();
  var showLabel = platform === VKCOM || platform === IOS && sizeX === SizeType.REGULAR;
  return /*#__PURE__*/React.createElement(PanelHeaderButton, _extends({
    className: classNames(getClassName('PanelHeaderBack', platform), {
      'PanelHeaderBack--has-label': showLabel && !!label
    }),
    label: showLabel && label
  }, props), platform === ANDROID ? /*#__PURE__*/React.createElement(Icon28ArrowLeftOutline, null) : platform === VKCOM ? /*#__PURE__*/React.createElement(Icon28ChevronLeftOutline, null) : /*#__PURE__*/React.createElement(Icon28ChevronBack, null));
};

export default /*#__PURE__*/React.memo(withAdaptivity(PanelHeaderBack, {
  sizeX: true
}));
//# sourceMappingURL=PanelHeaderBack.js.map